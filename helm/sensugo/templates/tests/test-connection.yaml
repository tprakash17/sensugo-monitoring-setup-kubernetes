apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sensugo.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "sensugo.name" . }}
    helm.sh/chart: {{ include "sensugo.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "sensugo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
